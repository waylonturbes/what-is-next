/** Nav for the `<AppLayout>` */
function NavBar() {
  return (
    <nav className="sticky bottom-0 w-full h-16 order-last border-t-1 bg-zinc-4 border-zinc-3 dark:bg-zinc-9 dark:border-zinc-7 sm:top-16 sm:left-0 sm:min-h-[calc(100vh-4rem)] sm:w-[calc(4rem-1px)] sm:order-none sm:border-r-1 sm:border-t-0">
      Nav
    </nav>
  );
}

export default NavBar;
